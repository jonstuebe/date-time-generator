import React from 'react';
import App from './App';

const AppContainer = React.createClass({
    render() {
        return (
            <div>
                <App />
            </div>
        );
    }
});

export default AppContainer;
